import React from 'react';

<div>
    <p>spinning the chamber and pulling the trigger! ...</p>
    <p>you're safe!</p>
    <p>BANG!!!!</p>
    <button onClick={handleClick}>Pull the trigger!</button>
</div>

let chamber = null
let spinningTheChamber = false

let bulletInChamber = 8

event.spinningTheChamber = true

timeout(2, seconds)
callback
random number for chamber >1 <9
state = new random number
and spinningTheChamber = false

if spinningTheChamber = true {
    <p>spinning the chamber and pulling the trigger! ...</p>
}

if chamber===bulletInChamber {
    <p>BANG!!!!</p>
}

else {
    <p>you're safe!</p>
}

Math.ceil(Math.random() * 8)

export default RouletteGun;